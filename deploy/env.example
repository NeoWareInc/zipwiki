# ZipWiki API (Fly / local Compose). Copy to deploy/.env — do not commit secrets.
# Phase 1b: health only. Leave Convex / Stripe / provider keys empty until later.

ZIPWIKI_PUBLIC_API_URL=http://localhost:3001
WEB_ORIGIN=http://localhost:5173,http://localhost:5174

# Later (new projects — not zipcodex.ai):
CONVEX_SITE_URL=
ZIPWIKI_WORKER_SECRET=
ANTHROPIC_API_KEY=
LLAMA_CLOUD_API_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_STANDARD=
STRIPE_PRICE_PRO=
