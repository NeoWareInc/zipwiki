# ZipWiki API — Fly prod (api.zipwiki.ai). Do not commit real values.
# Stripe and auth live on the Convex deployment, not on this process.

WEB_ORIGIN=https://zipwiki-web.vercel.app
CONVEX_SITE_URL=
ZIPWIKI_WORKER_SECRET=
ANTHROPIC_API_KEY=
LLAMA_CLOUD_API_KEY=
