diff --git a/dist/server/implementation/index.js b/dist/server/implementation/index.js
index 107e1b01a99216af4d422d9776e597430aa5ba66..576c453a49eb4bf254b1bdc5c227eb1f44360d5a 100644
--- a/dist/server/implementation/index.js
+++ b/dist/server/implementation/index.js
@@ -224,7 +224,10 @@ export function convexAuth(config_) {
                     }
                     catch (error) {
                         logError(error);
-                        return Response.redirect(destinationUrl);
+                        const googleError = params.get("error_description") || params.get("error");
+                        const message = googleError ||
+                            (error instanceof Error ? error.message : String(error));
+                        return Response.redirect(setURLSearchParam(destinationUrl, "error", String(message).slice(0, 400)));
                     }
                 });
                 http.route({
diff --git a/src/server/implementation/index.ts b/src/server/implementation/index.ts
index 3cb4bfcb4b6b7c02ced3159e53975df35a001ef6..c87434e45583f903ded0807ef1c3d02a699cd252 100644
--- a/src/server/implementation/index.ts
+++ b/src/server/implementation/index.ts
@@ -360,7 +360,18 @@ export function convexAuth(config_: ConvexAuthConfig) {
               });
             } catch (error) {
               logError(error);
-              return Response.redirect(destinationUrl);
+              const googleError =
+                params.get("error_description") || params.get("error");
+              const message =
+                googleError ||
+                (error instanceof Error ? error.message : String(error));
+              return Response.redirect(
+                setURLSearchParam(
+                  destinationUrl,
+                  "error",
+                  String(message).slice(0, 400),
+                ),
+              );
             }
           },
         );
