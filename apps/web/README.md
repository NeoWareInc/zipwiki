# @zipwiki/web

Static marketing site for [zipwiki.ai](https://zipwiki.ai).

```bash
pnpm --filter @zipwiki/web dev
pnpm --filter @zipwiki/web build
```

Routes: `/` `/product` `/how-it-works` `/pricing` `/roadmap` `/terms` `/privacy`.

CTAs: waitlist (`hello@zipwiki.ai`). No dashboard, auth, or Convex.
