# crates/

Placeholder for the Rust runtime.

**Phase 3:** native `zipwiki` / `zipaccess` binaries for select environments
(macOS arm64 first). TypeScript stays for MCP, Node, browser, and Vercel.

Done when one environment packs and catalogs a `.zipwiki` that TypeScript
zipaccess can open. Shared APPNOTE, two implementations.
