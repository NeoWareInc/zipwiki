# crates/

Placeholder for the Rust runtime.

**Phase 4 (after TypeScript Beta):** a native `zipwiki` binary
for machines we compile for (macOS arm64 first). TypeScript is the Beta and
the everywhere runtime — MCP, Node, browser, Vercel, and any OS without a
binary.

Done when one compiled environment packs and catalogs a `.zipwiki` that
the TypeScript zipaccess library can open. Shared APPNOTE, two implementations.
